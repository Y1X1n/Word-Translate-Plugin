&lt;Window x:Class="WordTranslationAddin.UI.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="翻译历史" 
        Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F3F2F1"
        MinWidth="500" MinHeight="400">
    
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#0078D4"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E1E1E1"/>
        <SolidColorBrush x:Key="TextPrimaryColor" Color="#323130"/>
        <SolidColorBrush x:Key="TextSecondaryColor" Color="#605E5C"/>
        <SolidColorBrush x:Key="TextTertiaryColor" Color="#8A8886"/>
        <SolidColorBrush x:Key="HoverBackgroundColor" Color="#F3F2F1"/>
        
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="36,8,12,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverBackgroundColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="HistoryItemStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        
        <Style x:Key="EmptyStateStyle" TargetType="StackPanel">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="{StaticResource BackgroundColor}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1">
            <Grid Height="56">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="翻译历史" FontSize="20" FontWeight="SemiBold" 
                           Foreground="{StaticResource TextPrimaryColor}"
                           VerticalAlignment="Center" Margin="24,0"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,16,0">
                    <Button x:Name="ClearAllButton" Style="{StaticResource IconButtonStyle}" 
                            Click="ClearAllButton_Click" ToolTip="清空历史">
                        <Path Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z" 
                              Fill="{StaticResource TextSecondaryColor}" Width="18" Height="18" Stretch="Uniform"/>
                    </Button>
                    <Button x:Name="CloseButton" Style="{StaticResource IconButtonStyle}" 
                            Click="CloseButton_Click" ToolTip="关闭">
                        <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                              Fill="{StaticResource TextSecondaryColor}" Width="18" Height="18" Stretch="Uniform"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 搜索栏 -->
        <Border Grid.Row="1" Background="{StaticResource BackgroundColor}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1">
            <Grid Margin="24,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Grid Grid.Column="0">
                    <TextBox x:Name="SearchBox" Style="{StaticResource SearchBoxStyle}"
                             TextChanged="SearchBox_TextChanged"/>
                    <Path Data="M9.5,3A6.5,6.5 0 0,1 16,9.5C16,11.11 15.41,12.59 14.44,13.73L14.71,14H15.5L20.5,19L19,20.5L14,15.5V14.71L13.73,14.44C12.59,15.41 11.11,16 9.5,16A6.5,6.5 0 0,1 3,9.5A6.5,6.5 0 0,1 9.5,3M9.5,5C7,5 5,7 5,9.5C5,12 7,14 9.5,14C12,14 14,12 14,9.5C14,7 12,5 9.5,5Z" 
                          Fill="{StaticResource TextTertiaryColor}" Width="16" Height="16" Stretch="Uniform"
                          HorizontalAlignment="Left" VerticalAlignment="Center" Margin="12,0"/>
                </Grid>
                
                <TextBlock x:Name="CountTextBlock" Grid.Column="1" 
                           Text="共 0 条记录" FontSize="13"
                           Foreground="{StaticResource TextSecondaryColor}"
                           VerticalAlignment="Center" Margin="16,0,0,0"/>
            </Grid>
        </Border>
        
        <!-- 历史列表 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="24,16">
            <StackPanel x:Name="HistoryListPanel">
                <!-- 动态添加历史记录项 -->
            </StackPanel>
        </ScrollViewer>
        
        <!-- 空状态 -->
        <StackPanel x:Name="EmptyStatePanel" Grid.Row="2" Style="{StaticResource EmptyStateStyle}" Visibility="Collapsed">
            <Path Data="M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2M12,4A8,8 0 0,1 20,12A8,8 0 0,1 12,20A8,8 0 0,1 4,12A8,8 0 0,1 12,4M12,6A6,6 0 0,0 6,12A6,6 0 0,0 12,18A6,6 0 0,0 18,12A6,6 0 0,0 12,6M12,8A4,4 0 0,1 16,12A4,4 0 0,1 12,16A4,4 0 0,1 8,12A4,4 0 0,1 12,8Z" 
                  Fill="{StaticResource BorderColor}" Width="64" Height="64" Stretch="Uniform" Margin="0,0,0,16"/>
            <TextBlock Text="暂无翻译历史" FontSize="16" Foreground="{StaticResource TextSecondaryColor}"/>
        </StackPanel>
        
        <!-- 底部状态栏 -->
        <Border Grid.Row="3" Background="{StaticResource BackgroundColor}" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,0,0">
            <TextBlock x:Name="StatusTextBlock" Text="就绪" FontSize="12"
                       Foreground="{StaticResource TextTertiaryColor}"
                       Margin="24,8"/>
        </Border>
    </Grid>
</Window>
